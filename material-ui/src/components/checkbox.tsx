import { Checkbox } from "@mui/material";

export const CheckboxComponent = () => {
  return (
    <div>
      <h2>Checkbox</h2>

      <Checkbox />
      <Checkbox defaultChecked />
      <Checkbox disabled />
    </div>
  );
};
